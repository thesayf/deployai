/** 9 · A4 the honest part (navy). Centered statement + fine print. */
export function HonestPart() {
  return (
    <section className="bg-navy a4">
      <div className="wrap">
        <h2 className="h3t">You own everything we produce.</h2>
        <p>
          Take the plan and build it in-house, or take it to another firm. That happens,
          and the diagnostic still did its job.
        </p>
        <p>
          The fee is not credited against later work. That keeps the recommendation
          honest: we have no reason to push the biggest build, and if a workflow is not
          worth automating yet, the report says so.
        </p>
      </div>
    </section>
  );
}
