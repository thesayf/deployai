Accordion is the M16 FAQ: full-width hairline rows, bold question left, rotating chevron right, smooth grid-rows collapse. Multiple items can be open at once. Usually sits on the field ground inside a `.faq` section.

```jsx
<Accordion items={[{ q: "How fast can we start?", a: "Most engagements start within two weeks." }]} />
```
