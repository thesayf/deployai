Footer renders the royal-blue link-column band (uppercase tracked column titles) plus the indigo legal strip. Default columns are the production Start/Firm/Trust graph.

```jsx
<Footer tagline="Anthropic Partner Network" />
```
